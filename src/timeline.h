#ifndef TIMELINE_H
#define TIMELINE_H

#include <pokeagb/types.h>
#include "config.h"

struct timeline {
};

void add_frame_data_to_timeline(struct timeline timeline, struct frame_data frame_data);

#endif